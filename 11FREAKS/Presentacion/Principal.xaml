<Window x:Class="_11FREAKS.Presentacion.Principal"
        xmlns:uc="clr-namespace:_11FREAKS.Presentacion"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:System="clr-namespace:System;assembly=System.Runtime"
        xmlns:SystemData="clr-namespace:System.Data;assembly=System.Data"
        xmlns:local="clr-namespace:_11FREAKS.Presentacion"
        mc:Ignorable="d"
        Title="Principal" Height="700" Width="1200" Closed="Window_Closed" WindowState="Maximized" Loaded="Window_Loaded" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Icon="/Resources/Logo11Freaks.png">


    <Window.Resources>
        <ResourceDictionary>
            <RoutedUICommand x:Key="Ctr1" Text="Texto Ayuda" />
            <RoutedUICommand x:Key="F1" Text="Another Text" />
        </ResourceDictionary>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="F1" Modifiers="Ctrl" Command="{StaticResource Ctr1}" />
        <KeyBinding Key="F2"  />
    </Window.InputBindings>

    <Window.CommandBindings>
        <CommandBinding Command="{StaticResource Ctr1}" Executed="CtrShortcut1" />
        <CommandBinding Command="{StaticResource F1}" Executed="F1Shortcut" />
    </Window.CommandBindings>


    <Grid>

        <Grid.Background>
            <ImageBrush ImageSource="/Resources/fondoMain.jpg" Stretch="UniformToFill"/>
        </Grid.Background>

        <Image x:Name="logo" Source="/Resources/Logo11Freaks.png" Opacity="0.85" Margin="606,50,-6,184" Width="200" Height="200" VerticalAlignment="Top" HorizontalAlignment="Right">
            <Image.ToolTip>
                <ToolTip Content="Desarrollado Por Miguel Díaz Osa sin fines lucrativos"/>
            </Image.ToolTip>
        </Image>

        
        <DataGrid x:Name="tablaOjeadores" Visibility="Collapsed" Height="450" Width="600" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" />
                <DataGridTextColumn Header="Equipo" Binding="{Binding Equipo}" />
                <DataGridTextColumn Header="Amarillas" Binding="{Binding Estadistica}" />
                <DataGridTextColumn Header="Partidos Jugados" Binding="{Binding PartidosJugados}" />
            </DataGrid.Columns>
        </DataGrid>




        <Menu Width="Auto" Height="50" Background="ForestGreen" VerticalAlignment="Top">
            <MenuItem x:Name="menuInicio" Header="INICIO" FontSize="20" Height="50" FontWeight="Heavy" Foreground="White">
                <MenuItem x:Name="menuCerrarSesion" Header="CERRAR SESIÓN" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuCerrarSesion_Click">
                    <MenuItem.ToolTip>
                        <ToolTip Content="CERRAR SESIÓN Y SALIR A PANTALLA INCIO"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuSalir" Header="SALIR" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuSalir_Click">
                    <MenuItem.ToolTip>
                        <ToolTip Content="SALIR DE 11FREAKS"/>
                    </MenuItem.ToolTip>
                </MenuItem>
            </MenuItem>

            <MenuItem x:Name="menuMiLiga" Header="MI LIGA" FontSize="20" Height="50" FontWeight="Heavy" Foreground="White">
                <MenuItem x:Name="menuMiClasificacion" Header="CLASIFICACIÓN" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuMiClasificacion_Click"  >
                    <MenuItem.ToolTip>
                        <ToolTip Content="VER CLASIFICACIÓN"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuVerEquipos" Header="VER EQUIPOS" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuVerEquipo_Click">
                    <MenuItem.ToolTip>
                        <ToolTip Content="CONSULTA LOS EQUIPOS DE LA LIGA"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuJugarPartido" Header="JUGAR PARTIDO" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuJugarPartido_Click" >
                    <MenuItem.ToolTip>
                        <ToolTip Content="DISPUTA PARTIDO"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuMercadoTraspasos" Header="MERCADO TRASPASOS" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuMercadoTraspasos_Click"   >
                    <MenuItem.ToolTip>
                        <ToolTip Content="IR A MERCADO TRASPASOS"/>
                    </MenuItem.ToolTip>
                </MenuItem>
            </MenuItem>


            <MenuItem x:Name="menuMiEquipo" Header="MI EQUIPO" FontSize="20" Height="50" FontWeight="Heavy" Foreground="White"  Click="menuVerEquipo_Click">
                <MenuItem.ToolTip>
                    <ToolTip Content="IR A MI EQUIPO"/>
                </MenuItem.ToolTip>
            </MenuItem>


            <MenuItem x:Name="menuOjeadores" Header="OJEADORES" FontSize="20" Height="50" FontWeight="Heavy" Foreground="White" >
                <MenuItem x:Name="menuVerClasificacion" Header="VER CLASIFICACIÓN" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuVerClasificacion_Click">
                    <MenuItem.ToolTip>
                        <ToolTip Content="ECHA UN VISTAZO A COMO LALIGA ACTUALMENTE EN LA REALIDAD"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuMaximosGoleadores" Header="TOP GOLEADORES" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuMaximosGoleadores_Click">
                    <MenuItem.ToolTip>
                        <ToolTip Content="ESTOS SON LOS GOLEADORES DE LALIGA EN LA REALIDAD"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuMaximosAsistentes" Header="TOP ASISTENTES" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuMaximosAsistentes_Click">
                    <MenuItem.ToolTip>
                        <ToolTip Content="ESTOS SON LOS MAYORES ASISTENTES DE LA LIGA"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuMaximosRojas" Header="TOP EXPULSADOS" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuMaximosRojas_Click">
                    <MenuItem.ToolTip>
                        <ToolTip Content="ESTOS SON LOS JUGADOERS MÁS EXPULSADOS DE LA LIGA"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuMaximosAmarillas" Header="TOP AMARILLAS" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuMaximosAmarillas_Click">
                    <MenuItem.ToolTip>
                        <ToolTip Content="ESTOS SON LOS JUGADOERS CON MÁS AMARILLAS DE LALIGA EN LA REALIDAD"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem.ToolTip>
                    <ToolTip Content="ECHA UN VISTAZO AL BOLETIN DE OJEADORES PARA VER COMO LALIGA ACTUALMENTE EN LA REALIDAD"/>
                </MenuItem.ToolTip>
            </MenuItem>


            <MenuItem x:Name="menuMiCuenta" Header="MI CUENTA" FontSize="20" Height="50" FontWeight="Heavy" Foreground="White" >
                <MenuItem x:Name="menuCambiarContraseña" Header="CAMBIAR CONTRASEÑA" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuCambiarContraseña_Click" >
                    <MenuItem.ToolTip>
                        <ToolTip Content="CAMBIAR CONTRASEÑA DE LA CUENTA"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuCambiarCorreo" Header="RESTABLECER CORREO" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuCambiarCorreo_Click"  >
                    <MenuItem.ToolTip>
                        <ToolTip Content="CAMBIAR DIRECCIÓN CORREO DE SU CUENTA"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuBorrarCuenta" Header="BORRAR CUENTA" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuBorrarCuenta_Click">
                    <MenuItem.ToolTip>
                        <ToolTip Content="BORRAR CUENTA DE FORMA DEFINITIVA"/>
                    </MenuItem.ToolTip>
                </MenuItem>
            </MenuItem>


            <MenuItem x:Name="menuAjustes" Header="AJUSTES" FontSize="20" Height="50" FontWeight="Heavy" Foreground="White">
                <MenuItem.ToolTip>
                    <ToolTip Content="ACCEDE A LA CONFIGURACIÓN DE 11FREAKS"/>
                </MenuItem.ToolTip>
                <MenuItem x:Name="menuOcultarLogo" Header="OCULTAR LOGO 11FREAKS" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuOcultarLogo_Click" >
                    <MenuItem.ToolTip>
                        <ToolTip Content="OCULTE EL LOGO DE 11FREAKS"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuAjustarVolumen" Header="AJUSTAR VOLUMEN" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuAjustarVolumen_Click"  >
                    <MenuItem.ToolTip>
                        <ToolTip Content="OCULTE EL LOGO DE 11FREAKS"/>
                    </MenuItem.ToolTip>
                </MenuItem>
            </MenuItem>

            <MenuItem x:Name="menuAyuda" Header="AYUDA" FontSize="20" Height="50" FontWeight="Heavy" Foreground="White" >
                <MenuItem.ToolTip>
                    <ToolTip Content="AQUÍ PODRÁS ENCONTRAR LA AYUDA QUE NECESITAS"/>
                </MenuItem.ToolTip>
                <MenuItem x:Name="menuDocumentacion" Header="DOCUMENTACIÓN" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuDocumentacion_Click"  >
                    <MenuItem.ToolTip>
                        <ToolTip Content="DOCUMENTACIÓN TÉCNICA DE 11FREAKS"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuAyudaUsuario" Header="AYUDA USUARIO" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuAyuda_Click" >
                    <MenuItem.ToolTip>
                        <ToolTip Content="GUÍA USUARIO DE 11FREAKS"/>
                    </MenuItem.ToolTip>
                </MenuItem>

            </MenuItem>


            <MenuItem  x:Name="menuOpcAdmin" Header="OPCIONES ADMIN"  FontSize="20" Height="50" FontWeight="Heavy" Foreground="White" IsEnabled="False" Visibility="Hidden">
                <MenuItem x:Name="menuAñadirAdmin" Header="AÑADIR ADMIN" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuAñadirAdmin_Click">
                    <MenuItem.ToolTip>
                        <ToolTip Content="OTORGUE PERMISOS DE ADMINISTRADOR A UN USUARIO"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuCrearUsuario" Header="CREAR USUARIO" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black">
                    <MenuItem.ToolTip>
                        <ToolTip Content="CREE UN USUARIO"/>
                    </MenuItem.ToolTip>
                </MenuItem>
                <MenuItem x:Name="menuConsultarUsuarios" Header="CONSULTAR USUARIOS" FontSize="15" Height="25" FontWeight="Heavy" Foreground="Black" Click="menuConsultarUsuarios_Click">
                    <MenuItem.ToolTip>
                        <ToolTip Content="HAGA CONSULTA GENÉRICA O INDIVIDUALIZADA DE LOS USUARIOS"/>
                    </MenuItem.ToolTip>
                </MenuItem>
            </MenuItem>

        </Menu>





        <Expander x:Name="expanderUsers" ExpandDirection="Left" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Column="0" Background="Gray" Visibility="Hidden">
            <Expander.Header>
                <TextBlock Text="Búsqueda Usuarios" VerticalAlignment="Center" FontSize="12" FontWeight="Bold">
                    <TextBlock.LayoutTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform Angle="90"/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </TextBlock.LayoutTransform></TextBlock>
            </Expander.Header>

            <uc:BusquedaUsuarios x:Name="control"/>
        </Expander>

        <Label x:Name="indicadorLbox" Content="" Visibility="Visible"  Margin="300,132,700,514" Height="38" Width="500" FontWeight="Black" FontSize="20" Foreground="White"/>
        <ListBox x:Name="miListBox" Visibility="Hidden" VerticalAlignment="Center" HorizontalAlignment="Center" Width="800" Height="350" SelectionChanged="miListBox_SelectionChanged">
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="OCULTAR" Click="MenuItem_Click"/>
                </ContextMenu>
            </ListBox.ContextMenu>
            <ListBox.ToolTip>
                <ToolTip Content="PUEDE OCULTAR ESTA VISTA PULSANDO CLICK DERECHO, O BUSCAR INFORMACIÓN DESDE CLASIFICACIÓN"/>
            </ListBox.ToolTip>
        </ListBox>





        <ListBox x:Name="jugadoresListBox" Height="600" Width="450" ToolTip="ESTA ES TU PLANTILLA" SelectionChanged="jugadoresListBox_SelectionChanged" HorizontalAlignment="Center" Margin="0,71,0,13">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="250"/>
                        </Grid.ColumnDefinitions>
                        <Image Source="{Binding Foto}" Width="100" Height="100" Margin="10" ClipToBounds="True">
                            <Image.Clip>
                                <EllipseGeometry RadiusX="50" RadiusY="50" Center="50,50"/>
                            </Image.Clip>
                        </Image>
                        <StackPanel Grid.Column="1" Margin="10">
                            <TextBlock Text="{Binding Nombre}" FontWeight="Bold" FontSize="23"/>
                            <TextBlock Text="{Binding Posicion}" FontStyle="Italic" FontSize="14">
                                <Run Text="    #"/>
                                <Run Text="{Binding Dorsal}"/>
                            </TextBlock>
                            <TextBlock Text="{Binding Equipo}"/>
                            <TextBlock FontSize="14" FontWeight="Bold">
                                <Run Text="{Binding Valor}"/>
                                <Run Text="€"/>
                            </TextBlock>
                        </StackPanel>
                        <Image Source="{Binding Escudo}" Width="50" Height="50" Grid.Column="2" HorizontalAlignment="Right" Margin="30,50"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.Resources>
                <Style TargetType="ListBox">
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="BorderThickness" Value="5"/>
                    <Setter Property="Background" Value="#E8E8E8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBox">
                                <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="25">
                                    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                          VerticalScrollBarVisibility="Hidden">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

            </ListBox.Resources>
        </ListBox>





        <ListBox Visibility="Visible" x:Name="equiposLigaListBox" Height="600" Width="450" ToolTip="ESTA ES TU PLANTILLA"  HorizontalAlignment="Center" Margin="0,71,0,13" SelectionChanged="equiposLigaListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="250"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="10">
                            <TextBlock FontWeight="Bold" FontSize="23">
                                <Run Text="{Binding Posicion}"/>
                                <Run Text="º"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10">
                            <TextBlock FontWeight="Bold" FontSize="23">
                                <Run Text="{Binding Puntos}"/>
                                <Run Text="pts"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Margin="10">
                            <TextBlock FontWeight="Bold" FontSize="20" Text="{Binding Nombre}"/>
                            <TextBlock FontStyle="Italic" Text="{Binding Abreviatura}"/>
                        </StackPanel>
                    </Grid>

                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.Resources>
                <Style TargetType="ListBox">
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="BorderThickness" Value="5"/>
                    <Setter Property="Background" Value="#E8E8E8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBox">
                                <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="25">
                                    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                          VerticalScrollBarVisibility="Hidden">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

            </ListBox.Resources>
        </ListBox>

























    </Grid>
</Window>

